import { AbstractPuzzle, PuzzleProperties } from '../../../Puzzle';

export class {{ puzzleClassName }} extends AbstractPuzzle {
  constructor(properties: PuzzleProperties) {
    super(properties);
  }

  resolve(input: any) {
    // Your magic happens here
  }
}
